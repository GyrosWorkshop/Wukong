FROM microsoft/dotnet:1.1.1-sdk
WORKDIR /dotnetapp

RUN mkdir -p /dotnetapp
COPY Wukong.sln .
COPY WukongNew ./WukongNew
RUN dotnet restore
# Publish to /dotnetapp/wukong-dist
RUN dotnet publish -c Release -o ../wukong-dist -r debian.8-x64

COPY appsettings.template.json .

COPY start.sh /
RUN chmod +x /start.sh

EXPOSE 5000
CMD ["/start.sh"]
